services:
  postgres:
    ports: []

  nginx:
    ports:
      - "${HOST_HTTP_PORT:-80}:80"
      - "${HOST_HTTPS_PORT:-443}:443"
    volumes:
      - ./infra/nginx.conf:/etc/nginx/nginx.conf:ro
      - ${NGINX_SSL_DIR:-./infra/ssl}:/etc/nginx/ssl:ro
